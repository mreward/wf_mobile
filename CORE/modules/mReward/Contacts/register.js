/**
 * @param dirname
 * @returns {{}}
 */
module.exports = (dirname) => {
    const PATH = `${dirname}/CORE/modules/mReward/Contacts`

    return {
        _screen_contacts: `${PATH}/pages/screen-contacts.vue`
    }
}
