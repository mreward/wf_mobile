/* DO NOT CHANGE: Auto-generated file */

import Vue from 'vue';

import InputNext from '_CORE/directives/InputNext';
import ImageCache from '_CORE/directives/ImageCache';
import InputMask from '_CORE/directives/InputMask';
import Analytics from '_CORE/directives/Analytics';
import GoogleMaps from '_CORE/directives/GoogleMaps';
import StatusBarFill from '_CORE/directives/StatusBarFill';
import HoldCallback from '_CORE/directives/HoldCallback';
import Browser from '_CORE/directives/Browser';
import ShowPassword from '_CORE/directives/ShowPassword';
import Clipboard from '_CORE/directives/Clipboard';

export default {
    init() {
        Vue.directive(InputNext.name || 'InputNext', InputNext);
        Vue.directive(ImageCache.name || 'ImageCache', ImageCache);
        Vue.directive(InputMask.name || 'InputMask', InputMask);
        Vue.directive(Analytics.name || 'Analytics', Analytics);
        Vue.directive(GoogleMaps.name || 'GoogleMaps', GoogleMaps);
        Vue.directive(StatusBarFill.name || 'StatusBarFill', StatusBarFill);
        Vue.directive(HoldCallback.name || 'HoldCallback', HoldCallback);
        Vue.directive(Browser.name || 'Browser', Browser);
        Vue.directive(ShowPassword.name || 'ShowPassword', ShowPassword);
        Vue.directive(Clipboard.name || 'Clipboard', Clipboard);
    },
};
