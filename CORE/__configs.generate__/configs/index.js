/* DO NOT CHANGE: Auto-generated file */

import onsenUI from './onsenUI';
import directives from './directives';
import filters from './filters';
import components from './components';
import plugins from './plugins';
import mixins from './mixins';

export default async function ({config}) {
    await onsenUI.init({config});
    await directives.init({config});
    await filters.init({config});
    await components.init({config});
    await plugins.init({config});
    await mixins.init({config});
}
